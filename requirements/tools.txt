# Host-agnostic tooling for the L0 layer — runs on Linux, macOS, and in CI.
# Deliberately free of any ROS dependency. See requirements/README.md.

# --- Model definition and validation ---
jsonschema==4.23.0          # L0 facility model schema validation
PyYAML==6.0.2               # model, manifest, and config parsing
pydantic==2.9.2             # typed model objects with validation at the boundary

# --- Generation ---
Jinja2==3.1.6               # templating for generated URDF/SDF/launch artifacts
                            # 3.1.4 carried PYSEC-2026-1472 (8.8) / -1475 / -1471

# --- Geometry and asset pipeline ---
numpy==2.1.3                # transforms, inertia tensor checks (linalg.eigvalsh
                            # covers positive-definiteness on its own)
scipy==1.14.1               # Qhull, for the convex-hull collision meshes ADR-0028
                            # decides on. PINNED HARD, and the pin is load-bearing
                            # rather than routine: a hull is a COMMITTED derived
                            # asset checked byte for byte against a fresh
                            # derivation, and Qhull may triangulate a coplanar
                            # facet differently between versions. Bumping this may
                            # therefore require `cite-model hulls --write` and a
                            # real diff in assets/meshes/ — which is the check
                            # working, not a fault. Was deliberately absent until
                            # 2026-08-31; `trimesh` below cannot compute a hull
                            # without it.
trimesh==4.5.3              # mesh inspection, decimation, convex hulls
lxml==6.1.2                 # URDF/SDF reading and writing with round-trip fidelity
                            # 5.3.0 carried PYSEC-2026-87 (7.5)

# --- CLI ---
typer==0.13.1               # tool entry points
rich==13.9.4                # readable diagnostics
