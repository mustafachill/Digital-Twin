---
# GENERATED by cite-model. DO NOT EDIT.
# Source:     model/
# Regenerate: ./scripts/validate-model --write
# A hand edit here is a Critical review finding (ADR-0004) and is detected by
# ./scripts/validate-model, which diffs this tree against a fresh generator run.
#
# Controller configuration for arm_3, loaded by the gz_ros2_control plugin
# in cell_a.urdf.xacro. Every name below is derived from the L0 model, which
# is what makes ADR-0005's guarantee structural: these are the same controller
# and joint names the physical arm will use, because there is nowhere else for
# them to come from.
/cite/cell_a/arm_3/controller_manager:
  ros__parameters:
    update_rate: 150  # Hz
    use_sim_time: true
    arm_3_joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster
    arm_3_gripper_controller:
      type: position_controllers/GripperActionController
    arm_3_joint_trajectory_controller:
      type: joint_trajectory_controller/JointTrajectoryController

/cite/cell_a/arm_3/arm_3_gripper_controller:
  ros__parameters:
    joint: arm_3_drive_joint
    max_effort: 60.0
    stall_timeout: 0.3
    stall_velocity_threshold: 0.001

/cite/cell_a/arm_3/arm_3_joint_trajectory_controller:
  ros__parameters:
    joints:
      - arm_3_joint1
      - arm_3_joint2
      - arm_3_joint3
      - arm_3_joint4
      - arm_3_joint5
    command_interfaces:
      - position
    state_interfaces:
      - position
      - velocity
