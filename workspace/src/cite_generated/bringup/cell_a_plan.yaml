---
# GENERATED by cite-model. DO NOT EDIT.
# Source:     model/
# Regenerate: ./scripts/validate-model --write
# A hand edit here is a Critical review finding (ADR-0004) and is detected by
# ./scripts/validate-model, which diffs this tree against a fresh generator run.
#
# The bring-up plan: what to start, in what order, and what each node needs to
# know. It is data rather than generated Python on purpose (P5) — the launch
# mechanism in cite_bringup is written once, reviewed once, and tested once;
# adding a fourth arm changes this list, not that code.
#
# `stage` expresses dependency, not time. The launch mechanism gates each stage
# on the previous one reporting success, never on a duration (P4).
plan:
  zone: cell_a
  world: package://cite_generated/worlds/cell_a.sdf
  scene: package://cite_generated/description/cell_a_scene.urdf.xacro
  static_frames: package://cite_generated/frames/cell_a_static_tf.yaml
  topology: package://cite_generated/topology/cell_a_flow.yaml

  controller_managers:
    - asset: arm_1
      node: /cite/cell_a/arm_1/controller_manager
      backend: sim
      # `simulator` means the controller manager is created inside the Gazebo
      # process by gz_ros2_control, so there is no separate process to wait on —
      # the spawner's service-availability wait is the synchronisation point.
      hosted_by: simulator
      description_topic: /cite/cell_a/arm_1/robot_description
      description: package://cite_generated/description/cell_a_arm_1.urdf.xacro
      # Where this model is placed in the world. Stated here rather than inside
      # the description, so the description says what the arm IS and the model
      # says where it stands (L1).
      spawn_xyz_m: "0 -0.35 0.6"
      spawn_rpy_rad: "0 0 1.57079633"
      parameters: package://cite_generated/control/cell_a_arm_1_controllers.yaml
      trajectory_action: /cite/cell_a/arm_1/arm_1_joint_trajectory_controller/follow_joint_trajectory
      gripper_action: /cite/cell_a/arm_1/arm_1_gripper_controller/gripper_cmd
      # The gripper's drive joint travels between these two values in its OWN
      # units. The skill server maps a task-space width onto it; without the
      # mapping a width in metres would be commanded as an angle.
      gripper_open_position: 0.0
      gripper_closed_position: 0.85
      # The linkage that converts between the two, as four dimensions of the end
      # effector rather than as a fitted curve:
      #   opening(q) = 2*(drive_pivot_y - pad_inset
      #                   + finger_offset_y*cos(q) - finger_offset_z*sin(q))
      # The widest opening is derived from these, never carried alongside them —
      # a declared maximum is a second place for one fact to be stated, and the
      # one it used to state (85 mm, against a true 88.93 mm) was wrong.
      gripper_drive_pivot_y_m: 0.035
      gripper_finger_offset_y_m: 0.035465
      gripper_finger_offset_z_m: 0.042039
      gripper_pad_inset_m: 0.026
      # The gripper controller's own goal tolerance, delivered to L3 because the
      # skill server needs it to judge a grasp: the controller ends a goal as soon
      # as |error| < goal_tolerance, so the position it reports is systematically
      # short of the command, and that bias has to be exceeded before a width
      # margin means anything. Same number the controller is configured with.
      gripper_goal_tolerance_rad: 0.01
      # What a Pick closes to when its goal names no width. Narrower than the
      # work-piece on purpose: a parallel gripper evidences a grasp by failing to
      # reach where it was sent, so a width equal to the part teaches the skill
      # nothing (ADR-0022). Its ceiling is derived and enforced in
      # cite_tools.validate.physical.
      gripper_default_grasp_width_m: 0.045
      controllers:
        - {name: arm_1_joint_state_broadcaster, stage: 0}
        - {name: arm_1_gripper_controller, stage: 1}
        - {name: arm_1_joint_trajectory_controller, stage: 1}
      moveit:
        group: arm_1_xarm5
        base_link: arm_1_link_base
        tip_link: arm_1_link_tcp
        # The home configuration comes from L0: where an arm rests between cycles
        # is a fact about this facility, not about the vendor's robot.
        home_rad: [0, -0.35, -1.05, 0, 1.4]
        srdf: package://cite_generated/moveit/cell_a_arm_1.srdf.xacro
        kinematics: package://cite_generated/moveit/cell_a_arm_1_kinematics.yaml
        ompl: package://cite_generated/moveit/cell_a_arm_1_ompl_planning.yaml
        joint_limits: package://cite_generated/moveit/cell_a_arm_1_joint_limits.yaml
        controllers: package://cite_generated/moveit/cell_a_arm_1_moveit_controllers.yaml
    - asset: arm_2
      node: /cite/cell_a/arm_2/controller_manager
      backend: sim
      # `simulator` means the controller manager is created inside the Gazebo
      # process by gz_ros2_control, so there is no separate process to wait on —
      # the spawner's service-availability wait is the synchronisation point.
      hosted_by: simulator
      description_topic: /cite/cell_a/arm_2/robot_description
      description: package://cite_generated/description/cell_a_arm_2.urdf.xacro
      # Where this model is placed in the world. Stated here rather than inside
      # the description, so the description says what the arm IS and the model
      # says where it stands (L1).
      spawn_xyz_m: "2.1 -0.35 0.6"
      spawn_rpy_rad: "0 0 1.57079633"
      parameters: package://cite_generated/control/cell_a_arm_2_controllers.yaml
      trajectory_action: /cite/cell_a/arm_2/arm_2_joint_trajectory_controller/follow_joint_trajectory
      gripper_action: /cite/cell_a/arm_2/arm_2_gripper_controller/gripper_cmd
      # The gripper's drive joint travels between these two values in its OWN
      # units. The skill server maps a task-space width onto it; without the
      # mapping a width in metres would be commanded as an angle.
      gripper_open_position: 0.0
      gripper_closed_position: 0.85
      # The linkage that converts between the two, as four dimensions of the end
      # effector rather than as a fitted curve:
      #   opening(q) = 2*(drive_pivot_y - pad_inset
      #                   + finger_offset_y*cos(q) - finger_offset_z*sin(q))
      # The widest opening is derived from these, never carried alongside them —
      # a declared maximum is a second place for one fact to be stated, and the
      # one it used to state (85 mm, against a true 88.93 mm) was wrong.
      gripper_drive_pivot_y_m: 0.035
      gripper_finger_offset_y_m: 0.035465
      gripper_finger_offset_z_m: 0.042039
      gripper_pad_inset_m: 0.026
      # The gripper controller's own goal tolerance, delivered to L3 because the
      # skill server needs it to judge a grasp: the controller ends a goal as soon
      # as |error| < goal_tolerance, so the position it reports is systematically
      # short of the command, and that bias has to be exceeded before a width
      # margin means anything. Same number the controller is configured with.
      gripper_goal_tolerance_rad: 0.01
      # What a Pick closes to when its goal names no width. Narrower than the
      # work-piece on purpose: a parallel gripper evidences a grasp by failing to
      # reach where it was sent, so a width equal to the part teaches the skill
      # nothing (ADR-0022). Its ceiling is derived and enforced in
      # cite_tools.validate.physical.
      gripper_default_grasp_width_m: 0.045
      controllers:
        - {name: arm_2_joint_state_broadcaster, stage: 0}
        - {name: arm_2_gripper_controller, stage: 1}
        - {name: arm_2_joint_trajectory_controller, stage: 1}
      moveit:
        group: arm_2_xarm5
        base_link: arm_2_link_base
        tip_link: arm_2_link_tcp
        # The home configuration comes from L0: where an arm rests between cycles
        # is a fact about this facility, not about the vendor's robot.
        home_rad: [0, -0.35, -1.05, 0, 1.4]
        srdf: package://cite_generated/moveit/cell_a_arm_2.srdf.xacro
        kinematics: package://cite_generated/moveit/cell_a_arm_2_kinematics.yaml
        ompl: package://cite_generated/moveit/cell_a_arm_2_ompl_planning.yaml
        joint_limits: package://cite_generated/moveit/cell_a_arm_2_joint_limits.yaml
        controllers: package://cite_generated/moveit/cell_a_arm_2_moveit_controllers.yaml
    - asset: arm_3
      node: /cite/cell_a/arm_3/controller_manager
      backend: sim
      # `simulator` means the controller manager is created inside the Gazebo
      # process by gz_ros2_control, so there is no separate process to wait on —
      # the spawner's service-availability wait is the synchronisation point.
      hosted_by: simulator
      description_topic: /cite/cell_a/arm_3/robot_description
      description: package://cite_generated/description/cell_a_arm_3.urdf.xacro
      # Where this model is placed in the world. Stated here rather than inside
      # the description, so the description says what the arm IS and the model
      # says where it stands (L1).
      spawn_xyz_m: "4.2 -0.35 0.6"
      spawn_rpy_rad: "0 0 1.57079633"
      parameters: package://cite_generated/control/cell_a_arm_3_controllers.yaml
      trajectory_action: /cite/cell_a/arm_3/arm_3_joint_trajectory_controller/follow_joint_trajectory
      gripper_action: /cite/cell_a/arm_3/arm_3_gripper_controller/gripper_cmd
      # The gripper's drive joint travels between these two values in its OWN
      # units. The skill server maps a task-space width onto it; without the
      # mapping a width in metres would be commanded as an angle.
      gripper_open_position: 0.0
      gripper_closed_position: 0.85
      # The linkage that converts between the two, as four dimensions of the end
      # effector rather than as a fitted curve:
      #   opening(q) = 2*(drive_pivot_y - pad_inset
      #                   + finger_offset_y*cos(q) - finger_offset_z*sin(q))
      # The widest opening is derived from these, never carried alongside them —
      # a declared maximum is a second place for one fact to be stated, and the
      # one it used to state (85 mm, against a true 88.93 mm) was wrong.
      gripper_drive_pivot_y_m: 0.035
      gripper_finger_offset_y_m: 0.035465
      gripper_finger_offset_z_m: 0.042039
      gripper_pad_inset_m: 0.026
      # The gripper controller's own goal tolerance, delivered to L3 because the
      # skill server needs it to judge a grasp: the controller ends a goal as soon
      # as |error| < goal_tolerance, so the position it reports is systematically
      # short of the command, and that bias has to be exceeded before a width
      # margin means anything. Same number the controller is configured with.
      gripper_goal_tolerance_rad: 0.01
      # What a Pick closes to when its goal names no width. Narrower than the
      # work-piece on purpose: a parallel gripper evidences a grasp by failing to
      # reach where it was sent, so a width equal to the part teaches the skill
      # nothing (ADR-0022). Its ceiling is derived and enforced in
      # cite_tools.validate.physical.
      gripper_default_grasp_width_m: 0.045
      controllers:
        - {name: arm_3_joint_state_broadcaster, stage: 0}
        - {name: arm_3_gripper_controller, stage: 1}
        - {name: arm_3_joint_trajectory_controller, stage: 1}
      moveit:
        group: arm_3_xarm5
        base_link: arm_3_link_base
        tip_link: arm_3_link_tcp
        # The home configuration comes from L0: where an arm rests between cycles
        # is a fact about this facility, not about the vendor's robot.
        home_rad: [0, -0.35, -1.05, 0, 1.4]
        srdf: package://cite_generated/moveit/cell_a_arm_3.srdf.xacro
        kinematics: package://cite_generated/moveit/cell_a_arm_3_kinematics.yaml
        ompl: package://cite_generated/moveit/cell_a_arm_3_ompl_planning.yaml
        joint_limits: package://cite_generated/moveit/cell_a_arm_3_joint_limits.yaml
        controllers: package://cite_generated/moveit/cell_a_arm_3_moveit_controllers.yaml

  conveyors:
    - asset: conveyor_1
      state_topic: /cite/cell_a/conveyor_1/state
      command_topic: /cite/cell_a/conveyor_1/command
      installed_speed_mps: 0.15
    - asset: conveyor_2
      state_topic: /cite/cell_a/conveyor_2/state
      command_topic: /cite/cell_a/conveyor_2/command
      installed_speed_mps: 0.15
    - asset: conveyor_3
      state_topic: /cite/cell_a/conveyor_3/state
      command_topic: /cite/cell_a/conveyor_3/command
      installed_speed_mps: 0.15

  sensors:
    - asset: beam_c1_out
      detection_topic: /cite/cell_a/beam_c1_out/detection
      beam_axis: y
      beam_length_m: 0.5
    - asset: beam_c2_out
      detection_topic: /cite/cell_a/beam_c2_out/detection
      beam_axis: y
      beam_length_m: 0.5
    - asset: beam_c3_out
      detection_topic: /cite/cell_a/beam_c3_out/detection
      beam_axis: y
      beam_length_m: 0.5
